# Supabase (client-safe — used in browser and server)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Supabase (server-only — never prefix with NEXT_PUBLIC_)
SUPABASE_SERVICE_ROLE_KEY=
